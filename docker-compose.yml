services:
  devpulse:
    build: .
    ports:
      # ホスト側のポートは .env の PORT で変更できます（既定 3000）
      - "${PORT:-3000}:3000"
    volumes:
      - ./data:/data:ro
    environment:
      - DATA_DIR=/data
    restart: unless-stopped
