## 1. 開発プロセスのリードタイム（PR Lifecycle）
PRの各フェーズの時間を計測することで、開発の「詰まり」を特定します。

| 指標名 | 定義・計算ロジック | 改善のヒント |
| :--- | :--- | :--- |
| **Pick-up Time** | `PR作成` から `最初のレビュー` までの時間 | 通知設定の最適化、レビュー時間の固定化 |
| **Review Duration** | `最初のレビュー` から `Approve` までの時間 | 議論の非同期化、または対面/ペアプロへの切り替え |
| **Deploy Lag** | `Approve` から `Mainブランチへのマージ` までの時間 | マージ権限の委譲、自動マージの検討 |
| **Total Cycle Time** | `最初のコミット` から `本番デプロイ` までの総時間 | 全体を通したボトルネックの特定 |



---

## 2. 変更の粒度とリスク（Work Size & Quality）
コードベースの健全性と、レビューのしやすさを測定します。

* **PR Size (LOC)**
    * **計算**: 1つのPRにおける `additions` + `deletions` の合計。
    * **目的**: レビュー負荷の軽減。500行を超えるとバグ発見率が低下する傾向があります。
* **Review Influence (Comments per PR)**
    * **計算**: PRごとの `review_comments` 数。
    * **目的**: レビューの形骸化防止。極端に低い場合は「差し戻し」が起きていない可能性があります。
* **Revert Rate**
    * **計算**: `Revert` を含むコミットメッセージの割合。
    * **目的**: 本番投入後の手戻りリスクの可視化。

---

## 3. チームの健全性と負荷（Collaboration）
特定の個人に依存せず、チーム全体でアウトプットを出せているかを測定します。

* **Active Days**
    * **計算**: コミット、PR作成、コメント等のアクションがあったユニークな日数/週。
    * **目的**: 会議過多による「コードを書く時間」の欠乏を検知。
* **Review Contribution Ratio**
    * **計算**: `PR作成数` と `レビュー実行数` の比率。
    * **目的**: 「レビュー専念者」や「投げっぱなし」の状態を防ぎ、知識の偏りを是正。
* **WIP (Work In Progress) per Developer**
    * **計算**: 1人あたりの `Open PR` 数。
    * **目的**: マルチタスクによるコンテキストスイッチの発生を抑制。
